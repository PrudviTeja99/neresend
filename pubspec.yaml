name: neresend
description: "NeReSend: A modern, privacy-first, cross-platform P2P file sharing application."
publish_to: 'none'
version: 1.0.11+15

environment:
  sdk: '>=3.3.0 <4.0.0'
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter
  
  # State Management
  flutter_riverpod: ^2.5.1

  # Security & Cryptography
  flutter_secure_storage: ^10.0.0
  cryptography: ^2.7.0
  crypto: ^3.0.3

  # Networking & WebRTC
  flutter_webrtc: ^1.6.1
  mobile_scanner: ^5.2.3

  # Device & File I/O
  file_picker: ^12.2.0
  permission_handler: ^12.0.0
  path_provider: ^2.1.2
  path: ^1.9.0

  # Utilities & Icons
  uuid: ^4.4.0
  intl: ^0.19.0
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

